
<div class="container">
    @*<table id="totalassets" class="table table-success">
        <thead>
        <th colspan="2"><h4>Total: Supply Assets</h4></th>
        </thead>
        <tbody>
            <tr>
                <td>Total:</td>
                <td>@Format.AsCurrency(totalAssets)</td>
            </tr>
        </tbody>
    </table>*@

    <table id="emptyitems" class="table table-striped">
        <thead class="table-info">
        <th colspan="3"><h4>Items - Low Stock</h4></th>
        <th></th>
        <th>Total</th>
        </thead>
        <tbody>
            @if (LowItems.Any())
            {
                @foreach (var item in LowItems)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Quantity</td>
                        <td>@item.Unit</td>
                        <td>@item.Price</td>
                        <td>@Calc.CalcItemTotal(item.Price, item.Quantity)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td><strong>No Low Items: Stock is good!</strong></td>
                </tr>
            }

        </tbody>
    </table>
</div>
